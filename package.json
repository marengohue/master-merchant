{
  "name": "master-merchant",
  "version": "0.0.0",
  "description": "The dwarven merchant game",
  "main": "index.js",
  "scripts": {
    "start": "npm run clean && npm run build && npm run launch",
    "clean": "rm -rf dist",
    "launch": "node ./dist/game.js",
    "build": "node ./node_modules/.bin/coffee --output dist/ --compile src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/marengohue/master-merchant"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "bugs": {
    "url": "https://github.com/marengohue/master-merchant/issues"
  },
  "devDependencies": {
    "coffeescript": "^2.3.0"
  }
}
